import React from 'react';
import { View, Styles, Button } from 'react-native';

const ColorScreen = () => {
    return (
        <View>
            <Button title="Add a Color" />
        </View>
    );
};

const styles = StyleSheet.create({});

export default ColorScreen;